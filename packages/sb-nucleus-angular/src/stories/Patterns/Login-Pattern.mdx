import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Patterns/Login Pattern" />

# Login pattern

See `ELEMENTS/Login Page` for a composed sample using atoms and molecules.

Patterns should capture realistic interaction flows, not only isolated components.
